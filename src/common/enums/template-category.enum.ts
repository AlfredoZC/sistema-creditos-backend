/**
 * Maps the PG enum type `template_category` (migration 1786000000003
 * WhatsAppBot). Value order matches the type declaration exactly.
 */
export enum TemplateCategory {
  UTILITY = 'utility',
  MARKETING = 'marketing',
  AUTHENTICATION = 'authentication',
}
