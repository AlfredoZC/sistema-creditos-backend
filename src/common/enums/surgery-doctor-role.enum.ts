/**
 * Maps the PG enum type `surgery_doctor_role` (migration 1786000000002).
 * Value order matches the type declaration exactly.
 */
export enum SurgeryDoctorRole {
  PRINCIPAL = 'principal',
  ASSISTANT = 'assistant',
  ANESTHESIOLOGIST = 'anesthesiologist',
}
