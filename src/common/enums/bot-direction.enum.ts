/**
 * Maps the PG enum type `bot_direction` (migration 1786000000003 WhatsAppBot).
 * Value order matches the type declaration exactly.
 */
export enum BotDirection {
  INBOUND = 'inbound',
  OUTBOUND = 'outbound',
}
